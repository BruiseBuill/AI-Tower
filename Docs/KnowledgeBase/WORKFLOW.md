# 开发流程与状态

## AI 开发流程

1. 读取根目录 `AGENTS.md` 和本知识库相关章节。
2. 明确本次任务的玩家价值、范围和手动验收条件。
3. 检查现有代码与场景，优先扩展 `BaseFramework`，避免重复系统。
4. 先实现最小可玩闭环，再增加表现和扩展点。
5. 使用稳定内容 ID 和占位资源完成表现，不绑定最终素材。
6. 确认可用的编译/Console 状态，不以历史日志冒充当前验证。
7. 更新本文件的实现状态以及必要的决策记录。
8. 向用户说明改动、当前限制和短小的手动验收步骤。

## 用户手动验收模板

每个功能交付时应给出类似以下内容：

1. 用项目指定的 Unity 版本打开工程和指定入口场景。
2. 清空或观察 Console，进入 Play Mode。
3. 按明确操作完成成功路径。
4. 按明确操作覆盖至少一个失败或边界路径。
5. 退出并重新进入，若涉及存档则确认状态是否保持。
6. 涉及触屏差异时，在 Android 设备上重复关键步骤。

不是所有任务都需要六步；应根据改动缩减。

## 当前实现状态（2026-08-29）

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 工程与渲染 | 已有基础 | Unity 2022.2，URP 2D。 |
| BaseFramework | 对象架构已首轮改良 | Control + Component + Data 已修复并被垂直切片
实际使用（士兵/塔/大本营）；`PoolManager`、`InputManager` 经实战验证，其余模块仍待审查。 |
| 输入 | 点击部署已落地 | `Game.Input.BattleInputManager` 继承 `BF.InputManager`；鼠标左键/单指点击战场会转为世界坐标并部署，触屏与分辨率适配待真机验证。 |
| 音频 | 雏形 | 已有管理器，尚无本游戏内容接入。 |
| 对象池 | 已实战 | 以预制体名称为键；战斗层经 `CombatPool` 统一封装。 |
| 场景切换 | 雏形 | 已有附加场景与淡入淡出逻辑。 |
| 存档 | 最小可用 | `Game.Save.SaveService`（schemaVersion=1）：关卡通关记录、
胜场、最佳剩余时间；胜利结算自动写入。 |
| 玩法 | 直线推进与战斗清理已落地 | 三种兵种按能量消耗阈值选择并扣费，点击处生成后直线前往大本营，靠近塔时转向并优先攻击；战斗结束和重试会清理士兵、塔及炮弹；入口场景 `Assets/Scenes/Level001.unity`。
2026-08-25 已通过批处理 PlayMode 实测（见下方“运行时验证记录”）。 |
| 成长 | 未开始 | 具体设计待确认。 |
| UI | 战斗 HUD 已有并扩展 | 能量条按三种 `SoldierData.TierColor` 变色，显示当前可部署兵种和消耗，保留倒计时、暂停与结算面板；尚无菜单与成长界面。 |
| 素材替换 | 部分落地 | 定义资产持稳定 contentId；表现走预制体 `VisualRoot` 契约；
目录校验工具尚未实现。 |
| 正式内容 | 未开始 | 由用户提供素材；关卡目标约十余关。 |
| 编辑器工具 | 已建立并扩展 | `AIOnly/构建垂直切片场景` 一键重建场景、预制体与定义资产；
Unity MCP 已接入，恢复脚本为 `Docs/Tools/Install-UnityMcp.ps1`（见 `TECHNICAL.md`）。 |

### 2026-08-29 点击部署与三档能量状态

- 已完成 `BattleInputManager : BF.InputManager`，复用基础 `onClick` 事件并将屏幕坐标转换为世界坐标。
- 已完成 `BattleFlow.TryDeployAt`：按当前能量选择可负担的最高阶兵种，扣除其 `EnergyCost`，在点击位置生成并直线移动。
- 已完成 `SoldierMovementComponent` 的直线推进和塔吸引；攻击模块在攻击范围内按塔优先、大本营兜底索敌。
- 已完成 `CombatRegistry.CloseAll` 与战斗清理流程，覆盖结算、重试、场景销毁和新战斗装配。
- 已完成三种生产兵种：突击兵、重装兵、精英兵；定义资产、独立 Prefab、对象池和首关部署列表均已由构建器生成。
- 已完成 HUD 能量条颜色和部署状态联动，能量不足时点击不生成。
- 已通过 Unity MCP 运行态检查：屏幕点击事件生成重装兵，位置为点击对应世界坐标，能量正确扣除，Console 无项目错误。

### 2026-08-29 架构重构状态

- 已完成士兵/防御塔对象目录拆分；配置数据为 `SoldierData` / `TowerData` ScriptableObject，
  运行时状态为 `SoldierRuntimeData` / `TowerRuntimeData`。
- 已完成独立模块：`SoldierMovementComponent`、`SoldierMeleeAttackComponent`、
  `TowerTargetingComponent`、`TowerProjectileAttackComponent`。
- 已用 Unity MCP 执行构建菜单并核对 Prefab 层级：突击兵、重装兵、精英兵、基础塔和炮弹均引用
  新类型；关卡引用已迁移到 `SoldierData` / `TowerData`；全部关卡数据校验通过；当前
  编辑器 Console 无自有编译错误。
- Play Mode 已确认可进入；本次 MCP 会话能读取到编辑器处于运行态，但后台没有推进游戏帧，
  因此完整移动、炮击和胜负时序仍以既有批处理记录及用户聚焦 Game 视图后的手动验证为准。

## 当前入口与手动验收

入口场景：`Assets/Scenes/Level001.unity`（构建列表第 1 位）。

垂直切片手动验收步骤：

1. 打开工程，双击 `Assets/Scenes/Level001.unity`，点击 Play。
2. 成功路径：进入战斗后点击地图任意非 UI 位置，观察士兵在点击处生成；能量条
   随当前能量在蓝色（突击兵，50）、金色（重装兵，120）、红色（精英兵，180）
   三档间变化，底部状态显示当前可部署兵种与消耗；士兵直线向大本营移动，靠近塔时改变方向并优先攻击防御塔，
   塔发射子弹反击。
   在倒计时归零前摧毁大本营，应出现“胜利”结算面板。
3. 失败路径：不部署任何士兵，等倒计时归零，应出现“失败”结算面板。
4. 暂停：左上角“暂停”按钮应冻结战斗（`Time.timeScale=0`），可继续或重新开始。
5. 存档：胜利一次后退出 Play 再进入，右上角应出现“本关已攻克”标记；
   存档位于 `Application.persistentDataPath/GameSave.es3`。
6. 分别在能量约 100、120、180 时点击地图，确认生成的兵种依次为突击兵、重装兵、精英兵，
   且能量分别扣除 50、120、180；能量不足最低消耗时点击不生成。
7. 胜利结算后请顺手停止 Play 并确认编辑器能正常退出（批处理环境下曾发现
   与 ES3.Editor 钩子相关的退出冻结，见 `DECISIONS.md`；交互式编辑器未复现确认）。
8. 架构重构验收：打开 `AIOnly/关卡编辑器`，选择 `Def_Level_001`，确认能修改并保存
   塔列表、塔位和大本营位置；点击“校验当前关卡”应显示通过；点击“打开运行场景”应打开
   `Assets/Scenes/Level001.unity`。随后执行 `AIOnly/构建垂直切片场景`，确认 Prefab
   组件与本文列出的新类型一致。
9. 尚未由真人验证的部分：真机触屏体验、不同分辨率下点击坐标与 UI 遮挡边界、难度手感、
   暂停/重试按钮的触屏手感。

## 运行时验证记录（2026-08-25，旧路径版本交接参考）

以下记录对应本次直线推进改造前的版本，保留用于历史交接，不能作为当前移动规则的依据。

曾使用临时编辑器脚本 `SmokeTestRunner`（验证完成后已按规则删除）在
`-batchmode -nographics` PlayMode 中完整跑通 Level001，确认：

- 能量随时间恢复；部署扣能量；突击兵(50)/重装兵(120) 均可部署。
- 士兵沿路径推进，进入射程后停下攻击：三座塔先后被摧毁，大本营血量下降至 0。
- 塔发射追踪子弹击杀士兵。
- 倒计时归零前摧毁大本营 → `Won`（实测约 87 秒获胜）；不部署等归零 → `Lost`。
- 胜利自动写存档，重新加载可读回（`completed/wins/timeLeft` 正确）。

注意两条批处理环境经验（细节见 `DECISIONS.md`）：

1. 强制杀掉批处理 Unity 进程可能损坏 Bee 构建缓存，导致后续批处理“找不到
   新加的方法/类型”。处理：重跑一次，或先执行一次 `BuildAll` 触发重新编译。
2. 批处理中调用过 ES3 保存后，退出 PlayMode 会冻结（`ES3.Editor.dll` 的
   `playModeStateChanged`/`EditorApplication` 钩子在无头环境异常）。批处理验证
  必须以外部看门狗强杀收尾；交互式编辑器是否受影响待用户确认。

## 2026-08-29 本次修改验证

- 已通过 Unity MCP 刷新并完成脚本编译；当前 Console 无项目编译错误。
- 已通过 Unity MCP 执行 `AIOnly/构建垂直切片场景`，并确认场景层级中不再有
  `PathVisual` 或 `SpawnMarker`。
- 已通过 Unity MCP 执行全部关卡数据校验，结果为通过。
- Play Mode 中已通过 MCP 调用部署入口，确认 `Playing` 状态下生成士兵且注册表为
  玩家 1、敌方 4；重试清理未再产生对象池重复回收错误。
- 未能通过 MCP 完成场景重载后的连续时序验证：编辑器在 MCP 调用 `Retry` 后停留于
  `playmode_transition`，随后已由 MCP 停止 Play Mode。直线转向、胜负后重新进入战斗、
  Android 触屏仍需用户在 Unity Game 视图和真机手动验证。

数值调整入口：`Assets/Game/Content/Definitions/` 下的定义资产
（士兵/塔/大本营/子弹/关卡），改后无需改代码。
重建场景入口：菜单 `AIOnly/构建垂直切片场景`（会覆盖手工场景修改）。

## 推荐里程碑

### M0：工程基线

- 确认并清除当前编译错误。
- 对 BaseFramework 其余模块做最小风险审查和必要修复（对象架构已完成首轮改良）。
- 确定屏幕方向、逻辑分辨率和首个垂直切片规则。

### M1：最小垂直切片

- 一张地图、一种单位、一种塔、一种部署资源。
- 可完成点击/拖动部署、直线推进、塔优先攻击、胜负、重试与战斗对象清理。
- 最小 HUD、暂停和关卡进度保存。

### M2：数据与内容生产管线

- 稳定内容 ID、ScriptableObject 定义和校验工具。
- 便捷的占位/正式素材替换。
- 数据驱动关卡、单位和防御塔配置。

### M3：成长与关卡选择

- 用户确认后的成长系统。
- 关卡解锁、奖励、教学状态与存档迁移。

### M4：内容扩展与 Android 打磨

- 扩展至约十余关和多种单位/防御塔/地形。
- 真机触控、性能、生命周期、安全区与资源导入打磨。

里程碑顺序可以调整，但每一阶段都应保持可运行和可由用户试玩。
