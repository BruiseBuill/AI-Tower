# AIOnly AI 协作规则

本文件是所有 AI 在修改本项目之前必须阅读的入口。项目知识库索引位于
`Docs/KnowledgeBase/README.md`。

## 强制规则

1. 禁止 AI 执行任何 Git 或其他版本控制命令，包括但不限于初始化、状态查询、
   拉取、提交、推送、切换分支、合并、回滚、暂存和修改版本控制配置。
2. 禁止 AI 上传或下载项目、代码、资源及构建产物。未经用户明确授权，不访问网络
   获取包、资源或远程内容。
3. 修改前先阅读知识库中与任务相关的文档；修改后同步更新受影响的知识库内容。
4. `Assets/Scripts/BaseFramework` 必须保留。可以按任务逐步修改，但不得整体删除、
   绕过或替换；重大改造应先说明影响并得到用户确认。
5. 用户提供的正式美术、音乐和语音是最终素材。AI 默认只使用简单几何图形、纯色、
   Unity 内置资源或已有占位资源，不擅自生成最终素材。
6. 玩法数据、关卡数据和素材引用应尽量数据驱动，避免把关卡数值、资源路径或正式
   素材直接硬编码在运行时代码中。
7. 运行时资源必须通过稳定标识或统一目录引用，使占位素材可在不修改玩法代码的前提
   下替换。
8. 目标平台优先级为 Android；Windows 编辑器仅用于开发和鼠标模拟。交互必须同时
   支持单指触屏与鼠标左键，不能依赖键盘完成玩家流程。
9. 游戏仅为中文、单机。未经用户确认，不加入联网、账号、排行榜、广告、内购或
   多语言系统。
10. 不主动添加自动化测试、截图验收或复杂 CI。每次交付必须说明用户在 Unity 中可
    如何手动验证，并报告尚未验证的部分。
11. 不随意升级 Unity、渲染管线或第三方插件。版本变更必须作为独立任务，经用户明确
    同意后进行。
12. 不直接修改 `Library`、`Temp`、`Logs`、`obj` 等 Unity 生成目录。项目源文件只应
    位于 `Assets`、`Packages`、`ProjectSettings`、`Docs` 等受控目录。
13. 不得把当前知识库中的“待确认问题”自行当作已确定需求。若某项决定会显著影响
    玩法、存档兼容性或内容生产，应先向用户确认。

## 每次任务的最小流程

1. 阅读 `Docs/KnowledgeBase/README.md` 及其中相关文档。
2. 检查当前代码、场景和资源，区分已有实现与计划中的实现。
3. 进行范围明确、可回退的修改，避免无关重构。
4. 检查 Unity 控制台或可用编译日志；不得把旧日志直接当作当前结果。
5. 给出简短的手动验收步骤，并更新知识库中的实现状态或决策记录。

## 当前最高优先级

首个可运行的最小垂直切片已经建立（入口 `Assets/Scenes/Level001.unity`）。后续正式
玩法扩展应继续保持一张地图、直线推进与塔拦截、至少一种进攻单位、一种防御塔、最小部署
操作、胜负结算和最小存档闭环可独立运行。具体新增规则仍需用户确认，见
`Docs/KnowledgeBase/OPEN_QUESTIONS.md`。

## 当前架构基线（2026-08-29）

- 士兵与防御塔按对象拆分目录；静态配置是 `Game.Content.SoldierData` /
  `Game.Content.TowerData` ScriptableObject，运行时状态是
  `SoldierRuntimeData` / `TowerRuntimeData`。
- 当前生产配置资产为 `Def_Soldier_Basic_Data`、`Def_Soldier_Heavy_Data`、
  `Def_Soldier_Elite_Data` 和 `Def_Tower_Basic_Data`；旧的同名无 `_Data` 资产仍保留为兼容资源，
  但不再被首个关卡引用。
- 士兵直线移动、塔吸引、近战攻击，防御塔索敌、炮击分别是独立模块；对象控制器只负责
  初始化、生命周期和对外战斗契约。士兵靠近塔时塔优先于大本营。
- 战斗结束、重试和新战斗装配前会关闭已注册战斗实体并清理对象池，避免持久化
  `PoolManager` 中残留上一局士兵、塔或炮弹。
- 每个兵种和塔型使用独立 Prefab；首个关卡当前有突击兵、重装兵、精英兵三种兵种。
  战场输入由 `Game.Input.BattleInputManager` 继承 `BF.InputManager`，鼠标左键和单指触摸点击地图后，
  按当前能量可负担的最高阶兵种生成士兵。当前生产入口是
  `AIOnly/构建垂直切片场景`，关卡数据入口是 `AIOnly/关卡编辑器`。
- 旧的 `SoldierDefinition`、`TowerDefinition` 与旧模块类仅作为已有资产兼容壳；新增
  代码应依赖 `SoldierData` / `TowerData` 及新模块类，不再把运行时状态命名为配置数据。
